#Product cart APplication Using Vanill javascript
```
BEGIN

let dummy_data = {
    product :[
         {
      "id": "1",
      "name": "Wireless Mouse",
      "price": 29.99,
      "rating": 4.5
    },
    {
      "id": "2",
      "name": "Mechanical Keyboard",
      "price": 89.99,
      "rating": 4.7
    },
    ]
}

//Using the dummy data parsed to js object

//then used to modify html using DOM Manipulation

FUNCTION createProductCard(product)
    //returns card div template element with product values


FOR every element in product data array
    FUNCTION createProductCard(product)
//displays products in screen
END FOR

let cart = []

//Every product item contains Add to cart button when clicked the clicked item will be added to cart

FOR every cart item call  
    FUNCTION displayCart()
    //displays display name of product , price and quantity

//Cleart cart perform, emptying cart cart = []
//sort ascending , sorts cart array in ascending order
//sort descending , sort cart array in descending order

```